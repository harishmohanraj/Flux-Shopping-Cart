var keyMirror = require('react/lib/keyMirror');

// Define action constants

module.exports  = keyMirror({
	CART_ADD : null,
	CART_REMOVE : null,
	CART_VISIBLE : null,
	SELECT_PRODUCT : null,
	RECEIVE_DATA : null
});